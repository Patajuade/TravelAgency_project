package be.helha.common.interfaces;

/**
 * Interface used to update the view of lists, plane stages and hotel stages
 */
public interface IViewController {
    /**
     * updates the view of lists, plane stages and hotel stages
     */
    void update();
}
